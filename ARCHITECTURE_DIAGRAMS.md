# 🏗️ Architecture System - Diagrammes

## 📐 Architecture Générale

```
┌─────────────────────────────────────────────────────────────────┐
│                     CLIENT NAVIGATEUR                           │
│                   http://localhost:5173                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP/HTTPS
                              │ (Requests/Responses)
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   FRONTEND (React + Vite)                       │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  Pages (Login, Register, VerifyEmail)                  │  │
│  │  ↓                                                       │  │
│  │  Services (authService, api.js)                        │  │
│  │  ↓                                                       │  │
│  │  Hooks (useAuth Context)                               │  │
│  │  ↓                                                       │  │
│  │  Axios Interceptor (JWT Auto-Attach)                   │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  localStorage: { token, user }                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ JSON + JWT Token
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│               BACKEND (Spring Boot)                             │
│              http://localhost:8080                              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  CORS Filter (Validate Origin)                          │  │
│  │  ↓                                                       │  │
│  │  Security Filter (Validate JWT)                         │  │
│  │  ↓                                                       │  │
│  │  AuthController (/api/auth/*)                           │  │
│  │  │                                                       │  │
│  │  ├─ register()     → User + Email Service               │  │
│  │  ├─ login()        → JWT Token Generation               │  │
│  │  ├─ verify-email() → Email Verification                 │  │
│  │  ├─ me()           → User Profile (Protected)           │  │
│  │  └─ logout()       → Token Revocation                   │  │
│  │  ↓                                                       │  │
│  │  User Repository (Database Layer)                       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ SQL
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                  DATABASE (PostgreSQL)                          │
│               localhost:5432/GestionVisite                      │
│                                                                 │
│  Tables:                                                        │
│  ├─ user                                                        │
│  │  ├─ id (PK)                                                │
│  │  ├─ email (unique)                                         │
│  │  ├─ password (encrypted)                                   │
│  │  ├─ firstName, lastName                                    │
│  │  ├─ role (VISITEUR, ADMIN, etc.)                          │
│  │  ├─ emailVerified (boolean)                               │
│  │  └─ failedLoginAttempts                                   │
│  │                                                             │
│  └─ [Autres tables pour rendez-vous, visites, etc.]          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flux d'Authentification Détaillé

### 1️⃣ Inscription

```
User Input (Register Form)
    │
    ↓
┌─────────────────────────────────────┐
│  Register Component                 │
│  ├─ firstName, lastName             │
│  ├─ email, password                 │
│  └─ confirmPassword                 │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  authService.register(data)         │
│  POST /api/auth/register            │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  AuthController.register()          │
│  ├─ Validate email not used         │
│  ├─ Hash password (BCrypt)          │
│  ├─ Save User (emailVerified=false) │
│  ├─ Generate verification code      │
│  └─ Send email                      │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Response (201 Created)             │
│  {                                  │
│    "message": "Registration OK",    │
│    "user": {...},                   │
│    "requiresVerification": true     │
│  }                                  │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Redirect to VerifyEmail Page       │
│  Email from backend/console         │
└─────────────────────────────────────┘
```

### 2️⃣ Vérification Email

```
User Input (Verification Code)
    │
    ↓
┌─────────────────────────────────────┐
│  VerifyEmail Component              │
│  ├─ Email (pre-filled)              │
│  └─ Code input                      │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  authService.verifyEmail(email,code)│
│  POST /api/auth/verify-email        │
│  ?email=x&code=y                    │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  AuthController.verifyEmail()       │
│  ├─ Validate code                   │
│  ├─ Update User (emailVerified=true)│
│  ├─ Delete verification code        │
│  └─ Send confirmation email         │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Response (200 OK)                  │
│  {"message": "Email verified"}      │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Redirect to Login Page (2s)        │
└─────────────────────────────────────┘
```

### 3️⃣ Connexion

```
User Input (Email + Password)
    │
    ↓
┌─────────────────────────────────────┐
│  Login Component                    │
│  ├─ email                           │
│  └─ password                        │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  authService.login(data)            │
│  POST /api/auth/login               │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  AuthController.login()             │
│  ├─ Find user by email              │
│  ├─ Validate password (BCrypt)      │
│  ├─ Generate JWT token              │
│  └─ Reset failed login attempts     │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Response (200 OK)                  │
│  {                                  │
│    "token": "eyJhbGc...",           │
│    "user": {...}                    │
│  }                                  │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Frontend (Login.jsx)               │
│  ├─ Save token → localStorage       │
│  ├─ Save user → localStorage        │
│  ├─ Update useAuth context          │
│  └─ Redirect to /visitor            │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Dashboard Loaded                   │
│  User is Authenticated ✅           │
└─────────────────────────────────────┘
```

### 4️⃣ Requête Authentifiée

```
Any API Call (Protected Route)
    │
    ↓
┌─────────────────────────────────────┐
│  Axios Interceptor                  │
│  ├─ Get token from localStorage     │
│  ├─ Add header:                     │
│  │  Authorization: Bearer {token}   │
│  └─ Send request                    │
└─────────────────────────────────────┘
    │
    ↓
┌─────────────────────────────────────┐
│  Backend Security Filter            │
│  ├─ Extract token from header       │
│  ├─ Validate JWT signature          │
│  ├─ Check token not revoked         │
│  ├─ Extract user email from token   │
│  └─ Load user context               │
└─────────────────────────────────────┘
    │
    ├─ If Valid ─────────────────────→ ✅ Execute endpoint
    │
    └─ If Invalid ──────────────────→ ❌ Return 401 Unauthorized
                                          ↓
                                    Frontend clears token
                                    Redirect to login
```

---

## 🗄️ Database Schema

```
┌─────────────────────────────────┐
│          USER TABLE             │
├─────────────────────────────────┤
│ id (UUID) [PK]                  │
│ email (VARCHAR) [UNIQUE]        │
│ password (VARCHAR)              │
│ firstName (VARCHAR)             │
│ lastName (VARCHAR)              │
│ whatsapp (VARCHAR) [NULLABLE]   │
│ role (ENUM)                     │
│   ├─ VISITEUR                   │
│   ├─ SECRETAIRE                 │
│   ├─ AGENT_SECURITE             │
│   ├─ EMPLOYE                    │
│   └─ ADMIN                      │
│ emailVerified (BOOLEAN)         │
│ failedLoginAttempts (INTEGER)   │
│ createdAt (TIMESTAMP)           │
│ updatedAt (TIMESTAMP)           │
└─────────────────────────────────┘
        ↓
  [PK/FK Relations to:]
  - RendezVous (N:1)
  - Visite (N:1)
  - etc.
```

---

## 🔐 Security Layers

```
                Sécurité Application
┌───────────────────────────────────────────────┐
│                                               │
│  1. HTTPS/TLS (Transport)                     │
│     └─ Chiffrement en transit                 │
│                                               │
│  2. CORS (Cross-Origin)                       │
│     └─ Validé: localhost:5173 seulement       │
│                                               │
│  3. Authentication (JWT Tokens)               │
│     └─ Token signé et validé                  │
│                                               │
│  4. Authorization (Role-Based)                │
│     └─ Routes protégées par rôle              │
│                                               │
│  5. Password Security                         │
│     └─ BCrypt (10 rounds)                     │
│                                               │
│  6. Email Verification                        │
│     └─ Code temporaire (exp: 24h)             │
│                                               │
│  7. Rate Limiting (Login Attempts)            │
│     └─ Alerte admin après 3 tentatives       │
│                                               │
│  8. CSRF Protection (Disabled for JWT)        │
│     └─ Tokens stateless = pas besoin          │
│                                               │
│  9. Token Revocation (Logout)                 │
│     └─ Token blacklist service                │
│                                               │
│  10. SQL Injection Prevention                 │
│      └─ JPA parameterized queries             │
│                                               │
└───────────────────────────────────────────────┘
```

---

## 🔄 Component Interaction

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │
       ↓
┌──────────────────┐
│  App.jsx         │
│  (Landing)       │
└─────┬────────────┘
      │
      ├─→ ┌─────────────────┐
      │   │ /auth/login     │
      │   │ Login.jsx       │
      │   │ ├─ Form         │
      │   │ ├─ useAuth()    │
      │   │ └─ authService  │
      │   └─────────────────┘
      │
      ├─→ ┌─────────────────┐
      │   │ /auth/register  │
      │   │ Register.jsx    │
      │   │ ├─ Form         │
      │   │ ├─ Validation   │
      │   │ └─ authService  │
      │   └─────────────────┘
      │
      └─→ ┌─────────────────┐
          │ /visitor        │
          │ (Protected)     │
          │ ├─ ProtectedRoute
          │ ├─ useAuth()    │
          │ └─ Component    │
          └─────────────────┘
              │
              ├─ Accès seulement si:
              │  ├─ token valide
              │  ├─ user présent
              │  └─ role == VISITEUR
              │
              └─ Sinon:
                 └─ Redirect /auth/login
```

---

## 📊 Data Flow (Une Requête Protégée)

```
┌────────────┐
│  Component │ (e.g., VisitorDashboard)
└─────┬──────┘
      │
      │ useAuth() → {user, token}
      ↓
┌──────────────────────┐
│ API Service Call     │
│ authService.getX()   │
└─────┬────────────────┘
      │
      ↓
┌──────────────────────────────────────────────┐
│  api.js (Axios Instance)                     │
│  ├─ baseURL: localhost:8080/api              │
│  └─ Interceptor:                             │
│     ├─ Add header: Authorization: Bearer...  │
│     └─ Send request                          │
└─────┬───────────────────────────────────────┘
      │
      │ HTTP POST/GET/etc.
      ↓
┌──────────────────────────────────────────────┐
│  Network                                     │
│  Headers: {Authorization: "Bearer token"}    │
└─────┬───────────────────────────────────────┘
      │
      ↓
┌──────────────────────────────────────────────┐
│  Backend (Spring Boot)                       │
│  ├─ Security Filter                          │
│  │  ├─ Extract token                         │
│  │  ├─ Validate JWT                          │
│  │  └─ Set user context                      │
│  │                                           │
│  ├─ Controller                               │
│  │  ├─ @GetMapping, @PostMapping, etc.      │
│  │  ├─ User context available                │
│  │  └─ Return response                       │
│  │                                           │
│  └─ Repository                               │
│     └─ Query database                        │
└─────┬───────────────────────────────────────┘
      │
      │ JSON Response
      ↓
┌──────────────────────────────────────────────┐
│  Frontend                                    │
│  ├─ Mutation/Query handler                   │
│  ├─ Update state                             │
│  └─ Re-render component                      │
└──────────────────────────────────────────────┘
      │
      ↓
┌────────────┐
│   Browser  │ (User sees updated data)
└────────────┘
```

---

## 📱 Technology Stack

```
Frontend                Backend              Database
─────────────────────────────────────────────────────
React 19              Spring Boot 3.2       PostgreSQL 12+
Vite 5.0              Spring Security       -
Tailwind CSS 3.4      Spring Data JPA       
React Router 6        PostgreSQL Driver     
React Hook Form       JWT (jsonwebtoken)    
@tanstack/react-query Lombok                
Axios                 Flyway                
                      Maven                 
```

---

**Dernière mise à jour:** 24/12/2025
